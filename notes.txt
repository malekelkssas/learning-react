npx create-react-app (project name)  => create react app
npm install => look at the dependicies and libraries and modules inside package.json and install it

npm run (server name) => run react app

npx json-server --watch data/db.json --port 8000 => run json server